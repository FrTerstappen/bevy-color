[package]
name = "exampleWorkspace"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.16.0", default-features = false, features = [
    "bevy_color",
] }
bevy_color = { version = "0.16.1", default-features = false }
